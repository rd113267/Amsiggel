import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  playButtonContainer: {
    //position: 'absolute',
    //top: 0,
    // right: 0,
    // left: 0,
    // bottom: 0,
    // alignItems: 'center',
    // justifyContent: 'center',
  },
});
